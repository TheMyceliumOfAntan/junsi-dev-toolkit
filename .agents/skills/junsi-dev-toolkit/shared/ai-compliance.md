# AI 服从性指引

补充 RDD/DBF/CM 中未覆盖的通用规则。

## 用户提意见时

用户对你的方案/计划说"改一下"时：
1. 先停下，**不得直接改代码**
2. 更新方案描述，问"这样可以吗？"
3. 用户确认后再改
4. 跳过此流程 = 违规

## 验证输出规范

说"验证通过"时，必须同时满足：
- 实际执行了命令（不是肉眼检查/grep）
- 粘贴了命令输出（至少关键行）
- 明确说了"符合预期"或"不符合"
